import React from "react";
import {Typography} from "@mui/material";

export const NotFoundPage: React.FC = () => {
  return (
    <Typography variant="h3">Couldn't find that page sorry!</Typography>
  )
}